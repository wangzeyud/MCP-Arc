---
description: 版本进度表（v0.1–v0.5），确认改动落在正确版本范围、不超范围。v0.5 为规范对齐（MCP 2026-07-28）。
alwaysApply: false
enabled: true
updatedAt: 2026-09-21T11:00:00.000Z
provider:
---

# 当前进度

| 版本 | 状态 | 内容 |
|------|------|------|
| v0.1 | ✅ 完成 | stdio/SSE 传输、审计（SQLite+PG）、脱敏、限流、控制台、回放 |
| v0.2 | ✅ 完成 | LLM 辅助脱敏、控制台规则 CRUD、JSON/CSV 导出 |
| v0.3 | ✅ 完成 | 上游异常优雅处理（重连/重启）、审计写入异步化+超时降级、预置脱敏规则模板、审计回放 UI、优雅关闭 flush（不丢审计/不挂客户端） |
| v0.4 | ✅ 完成 | 稳定性 / soak 测试 + race 验证（内部质量，非新功能） |
| v0.5 | ✅ 已实现 | 规范对齐（MCP 2026-07-28）：新增 Streamable HTTP 传输（client+upstream，T1–T3）、方法无关透传、通知作用域与取消传播（T4–T5）、InputRequiredResult 审计/不嵌套脱敏（T6）、传输单测+soak 变体（T7）、文档/规则更新（T8）；SSE 标记 legacy。详见 `docs/v0.5-plan.md`。**T5 已完成并验证**：客户端断开→代理主动取消在途上游请求（`handleClientMessage` 异步转发，传输层在连接关闭时调用 `cleanup→cancelUpstream+dropPending`），已由真实代理级 E2E 测试 `TestE2EStreamableHTTPProxyCancelOnClientDisconnect` 覆盖。 |

> v0.5 传输层规范对齐已完成（T1–T8）。范围为传输层，不新增"四件套"之外功能。新改动如属于"配置热加载/告警 webhook/统计增强/7×24 soak"等曾被推迟项，需先确认其目标版本，不得擅自塞入后续迭代。
