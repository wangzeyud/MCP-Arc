---
description: 版本进度表（v0.1–v0.7），确认改动落在正确版本范围、不超范围。v0.5 为规范对齐（MCP 2026-07-28），v0.6 为四件套打磨（回放增强+审计保留），v0.7 为交付增强（配置热加载+告警 webhook+统计增强）。
alwaysApply: false
enabled: true
updatedAt: 2026-09-21T11:00:00.000Z
provider:
---

# 当前进度

| 版本 | 状态 | 内容 |
|------|------|------|
| v0.1 | ✅ 完成 | stdio/SSE 传输、审计（SQLite+PG）、脱敏、限流、控制台、回放 |
| v0.2 | ✅ 完成 | LLM 辅助脱敏、控制台规则 CRUD、JSON/CSV 导出 |
| v0.3 | ✅ 完成 | 上游异常优雅处理（重连/重启）、审计写入异步化+超时降级、预置脱敏规则模板、审计回放 UI、优雅关闭 flush（不丢审计/不挂客户端） |
| v0.4 | ✅ 完成 | 稳定性 / soak 测试 + race 验证（内部质量，非新功能） |
| v0.5 | ✅ 已完成 | 规范对齐（MCP 2026-07-28）：新增 Streamable HTTP 传输（client+upstream，T1–T3）、方法无关透传、通知作用域与取消传播（T4–T5）、InputRequiredResult 审计/不嵌套脱敏（T6）、传输单测+soak 变体（T7）、文档/规则更新（T8）；SSE 标记 legacy。详见 `docs/v0.5-plan.md`。**T5 已完成并验证**：客户端断开→代理主动取消在途上游请求，已由真实代理级 E2E 测试 `TestE2EStreamableHTTPProxyCancelOnClientDisconnect` 覆盖。 |
| v0.6 | ✅ 已完成 | 四件套打磨：**回放增强**——批量回放 API `/api/replay/batch`（按 id 列表或过滤器解析，顺序执行、单条失败不中断）+ 结果 Diff（exact JSON 比对，验证上游是否仍返回一致结果）；**审计保留**——`audit.retention`（`max_age_days`/`max_rows`，默认关闭、向后兼容），启动裁剪 + 周期后台清理 worker（跨 SQLite/PG/Memory，失败 fail-open 不阻塞主流程）；控制台 Logs 页支持多选回放 + Diff 面板。测试：audit/内存/代理三级 retention 单测、admin 批量回放单测。 |
| v0.7 | ✅ 已完成 | 交付增强：**配置热加载**——`config.Manager`（原子替换 + mtime 轮询 + OnReload 回调）+ `ratelimit.Reload` + 保留策略热加载 + `POST /api/config/reload`（单测通过）；**告警 webhook**——`alerting` 包（HMAC-SHA256 签名 + 异步 fail-open），5 类事件 `config_reloaded`/`restart_required`/`sensitive_detected`/`rate_limited`/`audit_error` 全部接入；**统计增强**——`latency_us` 列 + `computeStats` 统一聚合（P50/P95/P99 µs + 错误率 + 按 client/tool 维度 + 每日时间序列），前端重写展示。测试：config 热加载单测、alerting 发送/签名/失败单测、统计聚合单测。 |
| v0.8 | 🔧 进行中 | 7×24 soak（长稳验证，从 v0.7 顺延）：可配置 soak 测试 `TestProxySoakStability`（`internal/proxy/soak_test.go`）已实现并**验证通过**——支持时长（`-soak.dur`/`SOAK_DUR`，默认 20000 轮退化为原 v0.4 快速门禁）、故障注入（`-soak.failRate` 按上游 error 比例）、**reload 循环**（文件型 `config.Manager` 反复触发 `ReloadConfig`，QPS 变异证明限流器真重配）、**goroutine / 堆内存采样断言**（基线 + 可选 `-soak.memLimitMB` 硬失败）。用 `soakCountingStore`（只计数不保留）+ `noopRespond` 排除测试桩自身内存增长，使内存断言只反映 proxy 行为。flag 与 env 双配置。已验证：默认门禁 20000 轮通过；reload+5% 故障 8s 跑通（reloads=15、堆 2MB、无 goroutine 泄漏）。**soak + `-race` 已发现并修复一个真实并发 bug**：`ratelimit.TokenBucketManager.Allow` 无锁读 `enabled` 与 `Reload` 写它竞争 → 改 `enabled` 为 `atomic.Bool`，`-race` soak 现通过。**新增 Streamable HTTP 长连接变体 `TestProxySoakStreamableHTTP`**：套用真实 `StreamableHTTPClient`/`StreamableHTTPUpstream` 接线（mock upstream + keep-alive HTTP 客户端驱动），覆盖传输层（HTTP 服务、每请求 SSE 流、TCP 连接复用、请求关联），与 `TestProxySoakStability` 共享 `resolveSoakCfg`/`soakCountingStore`/reload 循环。已验证：默认门禁 20000 轮真实 HTTP 往返通过；reload+5% 故障 8s 跑通（calls=43579=upstreamHits、reloads=16、堆 2MB、无丢失/泄漏）；`-race` reload 6s 通过（masker 重载并发安全，且 `TokenBucketManager` 竞争已修）。下一步：① 真 24h 跑（CI/长时环境，丢 `SOAK_DUR=24h`）；② 对两 soak 套件跑 `-race` 长跑。纯内部质量，不引入新功能/新依赖。 |
| v0.9 | 🔲 规划 | 交付收尾（从 v0.8 顺延）：Dockerfile **非 root 用户**；反向代理 **HTTPS 部署指南**（NGINX/Caddy）+ **健康检查端点**（`/healthz` 或复用 `/api/status`）方便 k8s/Docker 探活。属交付/安全加固，非产品功能。 |

> **偏离项（需你显式确认才落地，不在当前规划内）**：分布式/HA（redis/etcd 共享状态后端，多实例共享限流/审计/回放——违反"轻量/零侵入"定位）；SSE upstream（代理到旧版 SSE MCP server，扩张传输矩阵）；二级告警通道（webhook 之外的 file/stdout/syslog）。策略引擎 / 多租户属更后续版本，明显偏离。

> v0.7 已确认目标版本，承接上述"配置热加载 / 告警 webhook / 统计增强"三项曾被推迟的改动（7×24 soak 仍属更后面版本，未纳入 v0.7）。新增改动若不属于这三项，需先确认其是否划入 v0.7，不得擅自塞入。

> **近期修复（会话内，未计入版本表）**：
> - **proxy stdio 启动竞态（第二节）**：客户端（Cherry Studio/Claude Desktop/Cursor）拉起 mcp-arc 即发 `initialize`，但上游子进程尚未连上，`writeUpstream` 直接返回 `upstream not connected`，握手判死。改为**首次连接前有限等待（10s）连上即转发、之后仍 fail-fast**（不阻塞重连间隙），配套 `internal/proxy/upstream_ready_test.go`。
> - **`config.yaml` 默认 `transport.client: stdio` + `admin.open_browser: false`**：本地 stdio 代理（模式 A）成为默认形态；不再每次被 MCP client 拉起时弹浏览器。
> - **CLI 上游解析（第四节-2）**：`--upstream` 改用引号感知切分（替代 `strings.Fields`），支持含空格的路径；配套 `cmd/mcp-arc/main_test.go`。
> - **配置发现顺序（第四节-3）**：`--config` > `MCP_ARC_CONFIG` 环境变量 > 可执行文件同目录 > 平台配置目录（Linux XDG / Windows %APPDATA% / macOS ~/Library/Application Support）。无配置时回退内置默认值，**不再强制弹浏览器**（仅日志打印控制台 URL）。
> - **回放 Diff "全 changed" bug 修复（v0.6 回放增强）**：`handleReplay` 返回的 `resp` 是完整 JSON-RPC 信封（`{jsonrpc,id,result}`），而库里 `RawResult` 仅存结果体（`result` 对象）。旧 `computeDiff` 直接对信封与结果体做 `DeepEqual`，永远不等 ⇒ 全部显示 changed。`computeDiff` 现先 `unwrapResult` 拆出 `.result`（或 `.error`）再比对；python/JS 端测试改用信封以贴近生产。配套 `TestComputeDiffUnwrapsEnvelope`。
> - **首次连接等待改为可配（第四节-3 衍生）**：硬编码 `10s` 改为 `server.upstream_connect_timeout_ms`（默认 30000，CLI `--upstream-connect-timeout-ms`）。npx 冷启动首次下载包常 >10s，现可调大到 60s；首次连上后仍 fail-fast。配套 `TestWriteUpstreamFirstConnectTimeoutConfigurable`。
> - **多实例 :8080 冲突 → 方案 A 原子占用（第四节第 5 项落地）**：旧 `resolvePorts` 用 `netutil.FreeTCPPort` 是"bind 即 close"的探测，多实例同时启动存在 TOCTOU 竞态（都测到 8080 空闲 → 真实 bind 才冲突）。新增 `netutil.ReserveTCPPort(host, preferred)`：先用 `portFree`（bind→释放→dial 的 Windows SO_REUSEADDR 探测）确认空闲，再 bind 并**持有不释放**该 listener，交给 `admin.Server.StartListener` 直接 serve，消除间隙。main `resolvePorts` 现返回持有的 admin listener 并经 `proxy.Options.AdminListener` 传入；`cfg.Admin.Port` 仍回写为真实端口以生成正确控制台 URL。`admin.Server` 新增 `StartListener`/`Serve`，原 `Start(port)` 保留（测试/回退）。配套 `TestReserveTCPPortHoldsAndMoves` / `TestReserveTCPPortKeepsPreferred` / `TestResolvePortsReservesAdminListener`。
> - **脱敏只应用首条规则（严重漏脱敏）**：`mask.maskString` 命中第一条匹配规则即 `return`，导致一个字符串只被一条规则处理——规则列表里 `email` 排第一时，同一句中的卡号 / api_key 全部漏遮，审计记录落库的是原文。改为**遍历所有命中规则依次叠加替换**（用 `ReplaceAllLiteralString`，mask 串按字面替换，避免 `$` 被当展开符）。配套 `TestMaskAppliesEveryMatchingRule`（正序）与 `TestMaskOrderDoesNotSkipRules`（反序），证明脱敏完整性不再依赖规则顺序。
